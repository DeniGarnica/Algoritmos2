#include<bits/stdc++.h>
using namespace std;

int main() {
  int num_ver;
  std::cin >> num_ver;
  while (num_ver!=0) {
  int X[num_ver], Y[num_ver];
    for (size_t i = 0; i < num_ver; i++) {
      std::cin >> X[i] >> Y[i];
    }

    std::cin >> num_ver;
  }
  return 0;
}
