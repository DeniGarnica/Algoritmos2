#include<bits/stdc++.h>
#define ll long long
int a[235];

int main(){
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(0);
  int num_casos, n, q;
  std::cin >> num_casos;
  for (int i = 0; i < num_casos; i++) {
    std::cin >> dato1;
  }
    return 0;
  }
